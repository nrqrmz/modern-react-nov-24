---
## Ejercicio

#### Pares e impares

Dado un array con `n` cantidad de numeros enteros, devolver 2 arrays distintos: uno con los valores pares y otro con los impares

```javascript
const randomNumbers = [7, -11, 5, 18, 3, 9, 14, 1, 6, -22];

const orderNumbers = (array) => {
    let evenNumbers = [];
    let oddNumbers = [];

    // Tu codigo acá
}
```

#### Extra
Recuerda que puedes hacer un [desestucturing](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Operators/Destructuring_assignment) del resultado de la función y dejar ambos arrays almacenados en distintas variables. Implementalo!
